import tw from 'twin.macro';

const TextInput = tw.input`py-3 px-5 border-2 rounded-full placeholder-gray-500 focus:outline-none focus:ring-4 focus:ring-blue-500 focus:ring-opacity-70`;

export default TextInput;
